# Releazio iOS SDK Example

Полнофункциональный пример интеграции Releazio iOS SDK в SwiftUI приложение. Демонстрирует все основные возможности SDK для управления обновлениями приложений.

## ✨ Демонстрируемые возможности

- ✅ **Конфигурация SDK** — Настройка с API ключом и локализацией
- ✅ **Проверка обновлений** — Использование метода `checkUpdates()`
- ✅ **Отображение UpdateState** — Показ состояния обновлений и типов
- ✅ **Changelog с WebView** — Отображение постов в встроенном браузере
- ✅ **Попапы обновлений** — Два стиля: Native iOS Alert и InAppUpdate
- ✅ **VersionView** — Компонент версии с кнопкой обновления
- ✅ **Локализация** — Поддержка русского и английского языков
- ✅ **Темная/светлая тема** — Переключение цветовых схем

## 🚀 Запуск примера

### 1. Откройте проект

```bash
cd Examples/ReleazioExample
open ReleazioExample.xcodeproj
```

### 2. Настройте API ключ

Откройте `ReleazioExampleApp.swift` и укажите свой API ключ:

```swift
let apiKey = "your-api-key-here"
```

### 3. Запустите приложение

- Выберите симулятор или устройство
- Нажмите `Cmd + R` для сборки и запуска

## 📋 Структура примера

### ReleazioExampleApp.swift

Точка входа приложения с конфигурацией SDK:

```swift
let configuration = ReleazioConfiguration(
    apiKey: apiKey,
    debugLoggingEnabled: true,
    locale: "ru"  // или "en"
)

Releazio.configure(with: configuration)
```

### ContentView.swift

Основной экран с демонстрацией всех функций:

#### Быстрые действия (Quick Actions)

- **Check Updates** — Проверка обновлений через `checkUpdates()`
- **Show Changelog** — Открытие changelog с WebView
- **Show Native Alert** — Показ попапа в стиле Native iOS
- **Show InAppUpdate Style** — Показ попапа в стиле InAppUpdate
- **Switch Theme** — Переключение светлой/темной темы

#### Статус обновлений (Update Status)

Отображает информацию из `UpdateState`:
- Наличие обновления
- Текущая и последняя версия
- Нужно ли показывать бейдж
- Нужно ли показывать попап
- Нужно ли показывать кнопку обновления
- Тип обновления (0, 1, 2, 3)

#### VersionView внизу экрана

Компонент версии приложения с кнопкой "Обновить" (если доступно обновление):

```swift
VersionView(
    updateState: updateState,
    locale: "ru",
    onUpdateTap: {
        Releazio.shared.openAppStore(updateState: updateState)
    }
)
```

## 💡 Основные примеры кода

### 1. Проверка обновлений

```swift
Task {
    isLoading = true
    defer { isLoading = false }
    
    do {
        let state = try await Releazio.shared.checkUpdates()
        await MainActor.run {
            updateState = state
            
            // Показываем попап если нужно
            if state.shouldShowPopup {
                showingUpdatePromptNative = true
            }
        }
    } catch {
        print("Ошибка проверки обновлений: \(error)")
    }
}
```

### 2. Отображение Changelog

```swift
Task {
    isLoading = true
    defer { isLoading = false }
    
    do {
        // Получаем конфиг из API
        let config = try await Releazio.shared.getConfig()
        
        // Извлекаем post_url из первого канала
        if let postURL = config.data.first?.postUrl {
            let changelog = Changelog(
                id: "1",
                title: "Changelog",
                content: postURL,  // В content хранится URL для WebView
                releaseId: "latest",
                createdAt: Date()
            )
            
            await MainActor.run {
                self.changelog = changelog
                showingChangelog = true
            }
        }
    } catch {
        print("Ошибка загрузки changelog: \(error)")
    }
}
```

### 3. Показ попапа (Native стиль)

```swift
ReleazioUpdatePromptView(
    updateState: state,
    style: .native,
    locale: "ru",
    onUpdate: {
        Releazio.shared.openAppStore(updateState: state)
        showingUpdatePromptNative = false
    },
    onSkip: { remaining in
        print("Пропущено, осталось: \(remaining)")
        showingUpdatePromptNative = false
    },
    onClose: {
        showingUpdatePromptNative = false
    },
    onInfoTap: {
        Releazio.shared.openPostURL(updateState: state)
    }
)
```

### 4. Показ попапа (InAppUpdate стиль)

```swift
ReleazioUpdatePromptView(
    updateState: state,
    style: .inAppUpdate,
    locale: "ru",
    onUpdate: {
        Releazio.shared.openAppStore(updateState: state)
        showingUpdatePromptInAppUpdate = false
    },
    onSkip: { remaining in
        print("Пропущено, осталось: \(remaining)")
        showingUpdatePromptInAppUpdate = false
    },
    onClose: {
        showingUpdatePromptInAppUpdate = false
    },
    onInfoTap: {
        Releazio.shared.openPostURL(updateState: state)
    }
)
.fullScreenCover(isPresented: $showingUpdatePromptInAppUpdate) {
    // Full screen для InAppUpdate стиля
}
```

### 5. Работа с UpdateState

```swift
if let state = updateState {
    // Проверка нужно ли показывать бейдж
    if state.shouldShowBadge {
        // Показать BadgeView
    }
    
    // Проверка нужно ли показывать попап
    if state.shouldShowPopup {
        // Показать ReleazioUpdatePromptView
    }
    
    // Проверка нужно ли показывать кнопку обновления
    if state.shouldShowUpdateButton {
        // Показать кнопку обновления
    }
    
    // Информация о версиях
    print("Текущая версия: \(state.currentVersionName)")
    print("Последняя версия: \(state.latestVersionName)")
    print("Тип обновления: \(state.updateType)")
    
    // Открытие App Store
    Releazio.shared.openAppStore(updateState: state)
    
    // Открытие поста
    Releazio.shared.openPostURL(updateState: state)
}
```

## 🎨 UI компоненты в примере

### ChangelogView

Модальное окно с WebView для отображения постов:

```swift
.sheet(isPresented: $showingChangelog) {
    NavigationView {
        ChangelogView(changelog: changelog) {
            showingChangelog = false
        }
    }
}
```

### ReleazioUpdatePromptView

Попап обновления с двумя стилями:

- **Native** — Модальное окно в стиле iOS Alert
- **InAppUpdate** — Полноэкранный стиль

### VersionView

Компонент версии, размещенный внизу экрана:

- Показывает текущую версию приложения
- Показывает кнопку "Обновить", если доступно обновление
- Поддерживает локализацию

## 🔧 Конфигурация

В примере используется следующая конфигурация:

```swift
ReleazioConfiguration(
    apiKey: apiKey,
    debugLoggingEnabled: true,      // Включены логи для отладки
    networkTimeout: 30,             // Таймаут 30 секунд
    analyticsEnabled: true,          // Включена аналитика
    cacheExpirationTime: 3600,       // Кэш на 1 час
    locale: "ru"                     // Русская локализация
)
```

## 🌍 Локализация

Пример поддерживает два языка:
- **ru** — Русский (используется по умолчанию в примере)
- **en** — Английский

Для изменения локализации измените параметр `locale` в `ReleazioConfiguration`.

## 📱 Типы обновлений

Пример демонстрирует работу со всеми типами обновлений:

1. **Type 0 (latest)** — Бейдж с возможностью открытия поста
2. **Type 1 (silent)** — Только кнопка обновления
3. **Type 2 (popup)** — Закрываемый попап
4. **Type 3 (popup force)** — Незакрываемый попап с пропусками

## 🐛 Отладка

### Включение логов

Логи включены по умолчанию в примере. Вы увидите в консоли:
- ✅ Успешную конфигурацию SDK
- 🔑 API ключ (первые 10 символов)
- 📊 События аналитики
- 🌐 Запросы к API

### Проверка UpdateState

В примере выводится подробная информация о состоянии обновлений:
- Наличие обновления
- Тип обновления
- Нужно ли показывать UI элементы
- Текущая и последняя версия

## 📖 Дополнительная документация

- [Основной README SDK](../README.md) — Полная документация SDK
- [API Documentation](../Documentation/API.md) — Справка по API
- [Integration Guide](../Documentation/Integration.md) — Руководство по интеграции

## 💡 Полезные советы

1. **Проверка обновлений** — Вызывайте `checkUpdates()` при запуске приложения или периодически
2. **Обработка ошибок** — Всегда оборачивайте вызовы SDK в `do-catch` блоки
3. **UI потоки** — Используйте `MainActor.run` для обновления UI из async функций
4. **Локализация** — Используйте параметр `locale` для поддержки нескольких языков
5. **Темная тема** — Используйте `.preferredColorScheme()` для переключения темы

## 🤝 Поддержка

- 📖 [Документация SDK](../README.md)
- 🐛 [GitHub Issues](https://github.com/Releazio/releazio-sdk-ios/issues)
- 📧 Email: support@releazio.com

## 📄 Лицензия

Этот пример проекта распространяется под лицензией MIT. Смотрите [LICENSE](../../LICENSE) для деталей.

---

**Приятной разработки! 🚀**
